<Window x:Class="Projekt.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Projekt.ViewModels"
        mc:Ignorable="d"
        Title="LoginView" Height="200" Width="300">
    <Window.DataContext>
        <viewmodels:LoginViewModel x:Name="ViewModel"/>
    </Window.DataContext>
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Username:" />
            <TextBox x:Name="UsernameBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Password:" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" />

            <TextBlock x:Name="ErrorText" Text="{Binding ErrorMessage, UpdateSourceTrigger=PropertyChanged}" Foreground="Red" />
            <Button Content="Login" Command="{Binding LoginCommand}" />
        </StackPanel>
    </Grid>
</Window>